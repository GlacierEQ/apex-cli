pe": "boolean"},
              "invokable": {"type": "boolean"},
              "verified": {"type": "boolean"},
              "latency_ms": {"type": ["number", "null"], "minimum": 0},
              "error": {"type": ["string", "null"]},
              "metadata": {"type": "object"}
            }
          }
        }
      }
    }
  }
}
<<<GLACIEREQ_FILE runtime/contracts/fallback_graph.json 521>>>
{
  "schema": "glaciereq.fallback-graph.v2",
  "contracts": {
    "equivalent": "Preserves required read/write, durability, authority, and verification semantics.",
    "degraded": "Can continue the mission with explicitly reduced semantics recorded in the receipt.",
    "read_only": "May satisfy reads but is forbidden for mutations.",
    "non_persistent": "May execute transiently but cannot satisfy durability requirements.",
    "forbidden": "Must never be selected as fallback for this edge."
  },
  "edges": []
}
<<<GLACIEREQ_FILE runtime/contracts/governor_defaults.json 360>>>
{
  "schema": "glaciereq.resource-governor-config.v2",
  "worker_slots": 8,
  "connector_concurrency": 4,
  "provider_concurrency": {
    "github": 2,
    "memory": 3,
    "model": 4
  },
  "token_budget": 200000,
  "retry_limit": 3,
  "deadline_seconds": 1800,
  "lease_ttl_seconds": 300,
  "rate_limit_backoff_cap_seconds": 300,
  "context_budget": 200000
}
<<<GLACIEREQ_FILE runtime/contracts/projection.schema.json 619>>>
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://glaciereq.local/schema/runtime-projection-v2.json",
  "title": "GlacierEQ Runtime Projection Manifest v2",
  "type": "object",
  "required": ["schema", "canonical_repository", "canonical_commit", "files", "generated_at"],
  "properties": {
    "schema": {"const": "glaciereq.runtime-projection.v2"},
    "canonical_repository": {"const": "GlacierEQ/apex-boot-core"},
    "canonical_commit": {"type": "string", "minLength": 7},
    "files": {"type": "object", "additionalProperties": {"type": "string", "pattern": "^[a-f0-9]{64}$"}}
  }
}
<<<GLACIEREQ_FILE runtime/contracts/receipt.schema.json 1134>>>
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://glaciereq.local/schema/runtime-receipt-v2.json",
  "title": "GlacierEQ Runtime Receipt v2",
  "type": "object",
  "required": [
    "schema",
    "receipt_id",
    "run_id",
    "status",
    "objective_sha256",
    "runtime",
    "capability_snapshot_sha256",
    "capability_snapshot",
    "action_journal_sha256",
    "source_predicates",
    "blockers",
    "continuation",
    "created_at",
    "action_journal_snapshot",
    "attestation_payload_sha256"
  ],
  "properties": {
    "schema": {
      "const": "glaciereq.runtime-receipt.v2"
    },
    "status": {
      "enum": [
        "READY",
        "DEGRADED",
        "BLOCKED",
        "FAILED"
      ]
    },
    "runtime": {
      "type": "object",
      "required": [
        "canonical_repository",
        "canonical_commit",
        "compiler_sha256",
        "contract_hashes"
      ]
    },
    "continuation": {
      "type": "object",
      "required": [
        "current_stage",
        "completed_stages",
        "outstanding_dependencies",
        "cursor"
      ]
    }
  }
}
<<<GLACIEREQ_FILE runtime/contracts/runtime_contract.json 1990>>>
{
  "schema": "glaciereq.runtime-contract.v2",
  "authority": "GlacierEQ/apex-boot-core",
  "receipt_schema": "glaciereq.runtime-receipt.v2",
  "capability_states": [
    "DISCOVERED",
    "CONNECTED",
    "AUTHENTICATED",
    "AUTHORIZED",
    "INVOKABLE",
    "VERIFIED"
  ],
  "runtime_stages": [
    "BOOT",
    "CAPABILITY_PROBE",
    "DISCOVER",
    "MAP",
    "REUSE",
    "EXTEND",
    "CHECKPOINT",
    "EXECUTE",
    "VERIFY",
    "PERSIST"
  ],
  "fallback_contracts": [
    "equivalent",
    "degraded",
    "read_only",
    "non_persistent",
    "forbidden"
  ],
  "completion": {
    "READY": "PERSIST completed and final receipt emitted",
    "DEGRADED": "meaningful execution completed with explicit reduced capability or stale/unavailable dependency",
    "BLOCKED": "no permitted repair, retry, or fallback crosses the current boundary",
    "FAILED": "runtime invariant or unrecoverable execution failure"
  },
  "projection_law": "Host adapters may change discovery mechanics and invocation syntax, but may not redefine capability states, lifecycle semantics, receipt fields, idempotency law, fallback semantics, or SLO measurements.",
  "freshness_law": "A capability that is outside its TTL is ineligible for runtime selection until re-probed, regardless of historical labels.",
  "idempotency_law": "A completed operation ID must never repeat its side effect. A pending operation requires target reconciliation before retry.",
  "replay_law": "Replay verifies compiler, contract, activation, skill, objective, and source-state predicates before attesting the receipt.",
  "resume_law": "Resume begins at the first incomplete runtime stage using the persisted continuation cursor and outstanding dependencies.",
  "capability_inventory_schema": "glaciereq.capability-inventory.v2",
  "resource_law": "Admission enforces worker/provider concurrency, token and context budgets, retry limit, run deadline, provider quota, rate-limit backoff, and priority inheritance."
}
<<<GLACIEREQ_FILE runtime/contracts/slo.json 330>>>
{
  "schema": "glaciereq.runtime-slo.v2",
  "thresholds": {
    "boot_latency_p95_ms_max": 5000,
    "state_recovery_latency_p95_ms_max": 1000,
    "composition_latency_p95_ms_max": 5000,
    "tool_success_rate_min": 0.95,
    "retry_rate_max": 0.20,
    "receipt_completeness_min": 1.0,
    "resume_success_rate_min": 0.99
  }
}
<<<GLACIEREQ_FILE runtime/hardening.py 16732>>>
#!/usr/bin/env python3
"""Completion layer for the canonical GlacierEQ runtime kernel v2.

The base compiler holds stable primitives. This module binds the remaining
admission, inventory, fallback, and attestation invariants without duplicating
host discovery logic.
"""
from __future__ import annotations

import json
import time
import uuid
from dataclasses import asdict
from datetime import datetime, timezone, timedelta
from pathlib import Path
from typing import Any, Mapping, Sequence

from runtime.compiler import (
    CAPABILITY_RANK,
    FINAL_STATUSES,
    FALLBACK_CONTRACTS,
    ActionJournal,
    CapabilityPlane,
    FallbackGraph,
    LifecycleEngine,
    MetricsRecorder,
    ReceiptManager,
    ResourceGovernor,
    RuntimeHealth,
    RuntimePaths,
    append_jsonl,
    atomic_write_json,
    parse_time,
    read_json,
    sha256_file,
    sha256_json,
    utc_now,
)


class CapabilityInventory:
    """Import host-known connector/tool/model identities without inventing liveness."""

    def __init__(self, plane: CapabilityPlane) -> None:
        self.plane = plane

    def import_inventory(self, inventory: Mapping[str, Any]) -> dict[str, Any]:
        records = []
        errors = []
        for item in inventory.get("capabilities", []):
            try:
                capability_id = str(item["capability_id"])
                kind = str(item["kind"])
                if kind not in {"connector", "tool", "model", "skill", "runtime", "memory", "service"}:
                    raise ValueError(f"unsupported kind: {kind}")
                record = self.plane.register(
                    capability_id,
                    kind,
                    str(item.get("provider", "unknown")),
                    ttl_seconds=int(item.get("ttl_seconds", 900)),
                    authorization_scopes=list(item.get("authorization_scopes", [])),
                    endpoint_ref=item.get("endpoint_ref"),
                    metadata=dict(item.get("metadata", {})),
                )
                probe = item.get("probe")
                if probe is not None:
                    checks = {
                        key: bool(probe.get(key, False))
                        for key in ("connected", "authenticated", "authorized", "invokable", "verified")
                    }
                    record = self.plane.record_probe(
                        capability_id,
                        checks,
                        latency_ms=probe.get("latency_ms"),
                        error=probe.get("error"),
                        metadata=probe.get("metadata", {}),
                    )
                records.append(record.to_dict())
            except Exception as exc:
                errors.append({"capability": item.get("capability_id"), "error": str(exc)})
        return {
            "schema": "glaciereq.capability-inventory-import.v2",
            "status": "VERIFIED" if not errors else "DEGRADED",
            "imported": records,
            "errors": errors,
        }


class ContractFallbackGraph(FallbackGraph):
    """Fallback selection that also enforces edge actions and scopes."""

    def choose(
        self,
        source_capability: str,
        *,
        action: str = "read",
        requires_persistence: bool = False,
        minimum_state: str = "VERIFIED",
        allow_degraded: bool = True,
    ) -> dict[str, Any] | None:
        candidates = [e for e in self.graph.get("edges", []) if e.get("from") == source_capability]
        for edge in sorted(candidates, key=lambda e: (e.get("priority", 100), e.get("to", ""))):
            contract = edge.get("contract")
            if contract not in FALLBACK_CONTRACTS or contract == "forbidden":
                continue
            allowed_actions = edge.get("actions")
            if allowed_actions and action not in allowed_actions:
                continue
            if contract == "read_only" and action != "read":
                continue
            if contract == "non_persistent" and requires_persistence:
                continue
            if contract == "degraded" and not allow_degraded:
                continue
            if edge.get("persistent") is False and requires_persistence:
                continue
            destination = edge.get("to")
            if not destination:
                continue
            try:
                if self.plane.eligible(
                    destination,
                    minimum_state=minimum_state,
                    required_scopes=edge.get("required_scopes", []),
                ):
                    return dict(edge)
            except KeyError:
                continue
        return None


class EconomicResourceGovernor(ResourceGovernor):
    """Enforce all declared runtime economics at admission time."""

    def __init__(self, paths: RuntimePaths, config: Mapping[str, Any] | None = None) -> None:
        config = dict(config or {})
        self.context_budget = int(config.get("context_budget", config.get("token_budget", 200_000)))
        super().__init__(paths, config)

    def _load(self) -> dict[str, Any]:
        state = super()._load()
        state.setdefault("context_tokens_used", 0)
        return state

    def record_provider_state(
        self,
        provider: str,
        *,
        quota_remaining: int | None = None,
        reset_after_seconds: int | None = None,
        metadata: Mapping[str, Any] | None = None,
    ) -> None:
        state = self._load()
        provider_state = state.setdefault("providers", {}).setdefault(provider, {})
        if quota_remaining is not None:
            provider_state["quota_remaining"] = int(quota_remaining)
        if reset_after_seconds is not None:
            provider_state["quota_reset_at"] = (
                datetime.now(timezone.utc) + timedelta(seconds=max(0, int(reset_after_seconds)))
            ).isoformat()
        if metadata:
            provider_state.setdefault("metadata", {}).update(dict(metadata))
        provider_state["updated_at"] = utc_now()
        self._save(state)

    def acquire(
        self,
        *,
        provider: str,
        operation: str,
        estimated_tokens: int = 0,
        estimated_context_tokens: int = 0,
        priority: int = 0,
        run_id: str | None = None,
        attempt: int = 1,
    ) -> dict[str, Any]:
        state = self._load()
        if int(attempt) > self.config.retry_limit + 1:
            return {"admitted": False, "reason": "retry_limit", "provider": provider, "attempt": attempt}

        if run_id:
            run = read_json(self.paths.run(run_id), {})
            started = parse_time(run.get("started_at"))
            if started and datetime.now(timezone.utc) > started + timedelta(seconds=self.config.deadline_seconds):
                return {"admitted": False, "reason": "deadline_exceeded", "provider": provider, "run_id": run_id}

        provider_state = state.setdefault("providers", {}).get(provider, {})
        quota_remaining = provider_state.get("quota_remaining")
        quota_reset_at = parse_time(provider_state.get("quota_reset_at"))
        if quota_remaining is not None and int(quota_remaining) <= 0:
            if not quota_reset_at or datetime.now(timezone.utc) < quota_reset_at:
                return {"admitted": False, "reason": "provider_quota", "provider": provider, "quota_remaining": quota_remaining}

        if int(state.get("context_tokens_used", 0)) + int(estimated_context_tokens) > self.context_budget:
            return {"admitted": False, "reason": "context_budget", "provider": provider}

        decision = super().acquire(
            provider=provider,
            operation=operation,
            estimated_tokens=estimated_tokens,
            priority=priority,
            run_id=run_id,
        )
        if not decision.get("admitted"):
            return decision

        lease_id = decision["lease"]["lease_id"]
        state = self._load()
        lease = state["leases"][lease_id]
        lease["attempt"] = int(attempt)
        lease["estimated_context_tokens"] = int(estimated_context_tokens)
        state["context_tokens_used"] = int(state.get("context_tokens_used", 0)) + int(estimated_context_tokens)
        self._save(state)
        decision["lease"] = lease
        return decision

    def release(self, lease_id: str, *, actual_tokens: int | None = None, actual_context_tokens: int | None = None) -> bool:
        state = self._load()
        lease = state.setdefault("leases", {}).pop(lease_id, None)
        if not lease:
            return False
        if actual_tokens is not None:
            state["tokens_used"] = max(
                0,
                int(state.get("tokens_used", 0))
                - int(lease.get("estimated_tokens", 0))
                + int(actual_tokens),
            )
        estimated_context = int(lease.get("estimated_context_tokens", 0))
        state["context_tokens_used"] = max(
            0,
            int(state.get("context_tokens_used", 0))
            - estimated_context
            + int(actual_context_tokens if actual_context_tokens is not None else estimated_context),
        )
        self._save(state)
        return True

    def snapshot(self) -> dict[str, Any]:
        state = super().snaps